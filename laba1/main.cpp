#include "colors.h"

#include <QApplication>

int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    Colors w;
    w.setMaximumSize(488,328);
    w.setMaximumSize(488,328);
    w.show();
    return a.exec();
}
